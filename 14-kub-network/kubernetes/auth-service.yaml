apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth
  type: ClusterIP
  ports:
    - protocol: 'TCP'
      port: 8080
      targetPort: 8080 # same port as on user-app.js
